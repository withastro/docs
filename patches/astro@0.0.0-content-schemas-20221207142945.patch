diff --git a/dist/content/vite-plugin-content.js b/dist/content/vite-plugin-content.js
index f6582ae7ee5f35da64fa45d0035e03b218047566..21f4905f28ddddc30c594afa3390973d21b05e63 100644
--- a/dist/content/vite-plugin-content.js
+++ b/dist/content/vite-plugin-content.js
@@ -1,9 +1,10 @@
 import { normalizePath } from "vite";
 import glob from "fast-glob";
 import * as fs from "node:fs/promises";
+import { getTimeStat } from '../core/build/util.js';
 import * as fsSync from "node:fs";
 import * as path from "node:path";
-import { bold, cyan } from "kleur/colors";
+import { bold, cyan, dim } from "kleur/colors";
 import { info, warn } from "../core/logger/core.js";
 import { appendForwardSlash, prependForwardSlash } from "../core/path.js";
 import { contentFileExts, CONTENT_FLAG, VIRTUAL_MODULE_ID } from "./consts.js";
@@ -216,8 +217,10 @@ export const _internal = {
           return;
         }
         info(logging, "content", "Generating entries...");
+        const timer = performance.now();
         contentGenerator = await createContentGenerator();
         await contentGenerator.init();
+        info(logging, "content", dim(`Completed in ${getTimeStat(timer, performance.now())}`));
       },
       async configureServer(viteServer) {
         if (contentDirExists) {
diff --git a/dist/core/build/vite-plugin-internals.js b/dist/core/build/vite-plugin-internals.js
index e03b64e8f3f61e85d9a14db927997f3357e3e197..0ad700f34f52be02f8f1865da18156263d2498eb 100644
--- a/dist/core/build/vite-plugin-internals.js
+++ b/dist/core/build/vite-plugin-internals.js
@@ -34,7 +34,11 @@ function vitePluginInternals(input, internals) {
           })
         );
       }
-      await Promise.all(promises);
+      console.log('----starting promises----');
+      for await (const p of promises) {
+        await p;
+      }
+      console.log('----promises resolved----');
       for (const [, chunk] of Object.entries(bundle)) {
         if (chunk.type === "chunk" && chunk.facadeModuleId) {
           const specifier = mapping.get(chunk.facadeModuleId) || chunk.facadeModuleId;