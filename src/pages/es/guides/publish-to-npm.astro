---
import { Markdown } from 'astro/components';
import MainLayout from '~/layouts/MainLayout.astro';

import readPage from "~/pages/en/guides/publish-to-npm.md"
const {frontmatter} = await readPage()
---

<MainLayout content={frontmatter}>
	<Markdown>
		> Esta página todavía no está disponible en Español. Se muestra la versión en inglés.

		<Markdown content={frontmatter.astro.source} />
	</Markdown>
</MainLayout>
