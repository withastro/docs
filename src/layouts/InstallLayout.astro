---
import MainLayout from './MainLayout.astro';
import InstallGuideTabGroup from '../components/TabGroup/InstallGuideTabGroup.astro';
import { getLanguageFromURL } from '../util.ts';

const installIntros = await Astro.fetchContent('../pages/*/install/_intro.md');
const lang = getLanguageFromURL(Astro.request.url.pathname);
const Intro = installIntros.filter(item => getLanguageFromURL(item.url) === lang)[0];
---

<MainLayout content={Astro.props.content}>
	{Intro && <Intro.Content />}
	<InstallGuideTabGroup />
	<slot />
</MainLayout>
