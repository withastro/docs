---
title: Menginstal plugin Vite atau Rollup
description: Pelajari cara mengimpor data YAML dengan menambahkan plugin Rollup ke proyek Anda.
i18nReady: true
type: recipe
---
import { Steps } from '@astrojs/starlight/components';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';

Astro dibangun di atas Vite, dan mendukung plugin Vite maupun Rollup. Resep ini menggunakan plugin Rollup untuk menambahkan kemampuan mengimpor berkas YAML (`.yml`) di Astro.

## Resep

<Steps>
1. Instal `@rollup/plugin-yaml`:

    <PackageManagerTabs>
        <Fragment slot="npm">
        ```shell
        npm install @rollup/plugin-yaml --save-dev
        ```
        </Fragment>
        <Fragment slot="pnpm">
        ```shell
        pnpm add @rollup/plugin-yaml --save-dev
        ```
        </Fragment>
        <Fragment slot="yarn">
        ```shell
        yarn add @rollup/plugin-yaml --dev
        ```
        </Fragment>
    </PackageManagerTabs>

2. Impor plugin di `astro.config.mjs` dan tambahkan ke array plugin Vite:

    ```js title="astro.config.mjs" ins={2,5-7}
    import { defineConfig } from 'astro/config';
    import yaml from '@rollup/plugin-yaml';

    export default defineConfig({
      vite: {
        plugins: [yaml()]
      }
    });
    ```

3. Terakhir, Anda bisa mengimpor data YAML menggunakan pernyataan `import`:

    ```js
    import yml from './data.yml';
    ```

    :::note
    Meskipun sekarang Anda dapat mengimpor data YAML di proyek Astro, editor Anda tidak akan menyediakan tipe untuk data yang diimpor. Untuk menambahkan tipe, buat atau temukan berkas `*.d.ts` di direktori `src` proyek Anda dan tambahkan berikut ini:
    ```ts title="src/files.d.ts"
    // Tentukan ekstensi file yang ingin Anda impor
    declare module "*.yml" {
      const value: any; // Tambahkan definisi tipe di sini jika diinginkan
      export default value;
    }
    ```
    Ini akan memungkinkan editor Anda memberikan hint tipe untuk data YAML Anda.
    :::
</Steps>