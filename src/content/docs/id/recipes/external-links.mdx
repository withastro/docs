---
title: Tambahkan ikon ke tautan eksternal
description: Pelajari cara memasang plugin rehype untuk menambahkan ikon ke tautan eksternal di file Markdown Anda.
i18nReady: true
type: recipe
---
import { Steps } from '@astrojs/starlight/components';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';

Dengan sebuah plugin rehype, Anda dapat mengidentifikasi dan memodifikasi tautan di file Markdown yang mengarah ke situs eksternal. Contoh ini menambahkan ikon di akhir setiap tautan eksternal, sehingga pengunjung tahu mereka sedang meninggalkan situs Anda.

## Prasyarat

- Proyek Astro yang menggunakan Markdown untuk halaman konten.

## Resep

<Steps>
1. Instal plugin `rehype-external-links`.

    <PackageManagerTabs>
        <Fragment slot="npm">
        ```shell
        npm install rehype-external-links
        ```
        </Fragment>
        <Fragment slot="pnpm">
        ```shell
        pnpm add rehype-external-links
        ```
        </Fragment>
        <Fragment slot="yarn">
        ```shell
        yarn add rehype-external-links
        ```
        </Fragment>
      </PackageManagerTabs>

2. Impor plugin ke dalam berkas `astro.config.mjs` Anda.

	Teruskan `rehypeExternalLinks` ke array `rehypePlugins`, bersama dengan objek opsi yang menyertakan properti `content`. Atur `type` properti ini ke `text` jika Anda ingin menambahkan teks biasa di akhir tautan. Untuk menambahkan HTML di akhir tautan, atur `type` menjadi `raw`.

    ```ts
    // ...
    import rehypeExternalLinks from 'rehype-external-links';

    export default defineConfig({
      // ...
      markdown: {
        rehypePlugins: [
          [
            rehypeExternalLinks,
            {
              content: { type: 'text', value: ' 🔗' }
            }
          ],
        ]
      },
    });
    ```

    :::note
      Nilai properti `content` [tidak direpresentasikan di accessibility tree](https://developer.mozilla.org/en-US/docs/Web/CSS/content#accessibility_concerns). Karena itu, sebaiknya jelaskan bahwa tautan tersebut eksternal pada konten di sekitarnya, alih-alih hanya mengandalkan ikon.
    :::
</Steps>


## Sumber Daya
- [rehype-external-links](https://www.npmjs.com/package/rehype-external-links)