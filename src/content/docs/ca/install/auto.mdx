---
title: Instal·la Astro amb el CLI automàtic
description: >-
  Com instal·lar Astro amb NPM, PNPM, o Yarn mitjançant create-astro amb el CLI
  d'Astro.
i18nReady: true
---
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro'

:::note[Prefereixes instal·lar Astro manualment?]
Llegeix la nostre [guia d'instal·lació manual pas a pas](/ca/install/manual/) en el seu lloc.
:::

#### Prerequisits

- **Node.js** - `v18.14.1` o major.
- **Editor de codi** - Recomanem [VS Code](https://code.visualstudio.com/) amb la nostra [extensió oficial d'Astro](https://marketplace.visualstudio.com/items?itemName=astro-build.astro-vscode).
- **Terminal** - Astro es utilitzant mitjançant la interfície de línia de comandos (CLI).

#### Instal·lació

`create astro` és la manera més ràpida i fàcil de començar un nou projecte en Astro. Seràs guiat pas a pas per configurar el teu nou projecte d'Astro. Podràs escollir entre algunes plantilles d'inici oficiales o pots [utilitzar qualsevol projecte en GitHub amb l'argument `--template`](#plantilles-de-inici).

:::tip[Previsualitzacions en línia]
Prefereixes provar Astro en el teu navegador? Visita [astro.new](https://astro.new/) per veure les nostres plantilles i fer-hi un projecte de Astro tan sols utilitzant el teu navegador.
:::

## 1. Executa l'Assistent de Configuració

Executa el següent comando en el teu terminal per iniciar l'assistent d'instal·lació:

<PackageManagerTabs>
  <Fragment slot="npm">
  ```shell
  # fes un nou projecte amb npm
  npm create astro@latest
  ```
  </Fragment>
  <Fragment slot="pnpm">
  ```shell
  # fes un nou projecte amb pnpm
  pnpm create astro@latest
  ```
  </Fragment>
  <Fragment slot="yarn">
  ```shell
  # fes un nou projecte amb yarn
  yarn create astro
  ```
  </Fragment>
</PackageManagerTabs>


Pots executar `create astro` en qualsevol carpeta del teu ordinador, així que no és necessari crear un directori buit abans d'inicialitzar el teu projecte. Si encara no tens un directori designat per al teu nou projecte, l'assistent en crearà un per a tu.

Si tot ha anat bé, hauries de veure un missatge "Liftoff confirmed. Explore your project!" seguit de algunes recomanacions de passos següents, "Next steps". Entra en el nou directori del teu projecte fent servir `cd` i comença a utilitzar Astro.

Si has omès el pas de `npm install` durant l'assistent de la interfície d'usuari de la línia de comandes (CLI), assegura't d'instal·lar les dependències abans de continuar.

## 2. Inicia Astro ✨

Astro posseeix un servidor de desenvolupament que té tot el que necessites per desenvolupar el teu projecte. La comanda `astro dev` iniciarà el servidor de desenvolupament local perquè puguis veure el teu nou projecte en acció per primera vegada.

Cada plantilla d'inici posseeix un script preconfigurat que executarà `astro dev` per a tu. Utilitza el teu gestor de paquets preferit per executar aquesta comanda i iniciar el servidor de desenvolupament d'Astro.

<PackageManagerTabs>
  <Fragment slot="npm">
  ```shell
  npm run dev
  ```
  </Fragment>
  <Fragment slot="pnpm">
  ```shell
  pnpm run dev
  ```
  </Fragment>
  <Fragment slot="yarn">
  ```shell
  yarn run dev
  ```
  </Fragment>
</PackageManagerTabs>

Si tot va bé, Astro hauria de servir el teu projecte localment a [http://localhost:4321](http://localhost:4321)!

Astro escoltarà qualsevol canvi a la carpeta `src/` i actualitzarà automàticament el teu projecte. D'aquesta manera, no serà necessari reiniciar el servidor local durant el desenvolupament.

Quan visualitzis el teu lloc al navegador, tindràs accés a la barra de eines de desenvolupament d'Astro. A mesura que construeixis, t'ajudarà a inspeccionar les teves illes, detectar problemes d'accessibilitat i molt més.

Si no és possible obrir el projecte al navegador, torna a la terminal on has executat la comanda dev i comprova si ha ocorregut algun error o si el teu projecte s'està servint en una URL diferent a la mencionada anteriorment.

## Plantilles de Inici

Pots començar un nou projecte d'Astro basat en un [exemple oficial](https://github.com/withastro/astro/tree/main/examples) o en la rama `main` de qualsevol repositori de GitHub passant un argument `--template` al comando `create astro`.

<PackageManagerTabs>
  <Fragment slot="npm">
  ```shell
  # fes un nou projecte a partir d'un exemple oficial
  npm create astro@latest -- --template <example-name>

  # fes un nou projecte basat en la rama `main` de un repositori de GitHub
  npm create astro@latest -- --template <github-username>/<github-repo>
  ```
  </Fragment>
  <Fragment slot="pnpm">
  ```shell
  # fes un nou projecte a partir d'un exemple oficial
  pnpm create astro@latest --template <example-name>
  
  # fes un nou projecte basat en la rama `main` d'un repositori de GitHub
  pnpm create astro@latest --template <github-username>/<github-repo>
  ```
  </Fragment>
  <Fragment slot="yarn">
  ```shell
  # fes un nou projecte a partir d'un exemple oficial
  yarn create astro --template <example-name>
  
  # fes un nou projecte basat en la rama `main` d'un repositori de GitHub
  yarn create astro --template <github-username>/<github-repo>
  ```
  </Fragment>
</PackageManagerTabs>

Per defecte, aquest comando utilitzarà la rama `main` del repositori de plantilla. Per utilitzar una rama diferent, passa-la com part de l'argumento `--template`: `<github-username>/<github-repo>#<branch>`.

Explora els nostres [temes i projectes d'inici](https://astro.build/themes/) on pots navegar per temes per blogs, portafolis, documentació, pàgines d'inici i més. O, [cerca en GitHub](https://github.com/search?o=desc&q=astro+starter&s=stars&type=Repositories) per més projectes d'inicio!

## Següents passos

Felicitats! Estàs preparat per començar a desenvolupar amb Astro! 🥳

Aquests son alguns temes que recomanem explorar després. Pots llegir-los en l'ordre que més t'agradi. També pots deixar la documentació a un costat i anar a jugar una mica amb el codi del teu nou projecte Astro i tornar quan tinguis algun problema o algun dubte.

📚 **Afegeix un framework:** Apren com estendre Astro amb suport per React, Svelte, Tailwind i més, utilitzant `npx astro add` en la nostra [Guia d'integracions](/ca/guides/integrations-guide/).

📚 **Desplega el teu web:** Apren com fer el build i desplegar el teu projecte d'Astro en la web amb la nostra [Guia de desplegament](/ca/guides/deploy/).

📚 **Enten el codi base:** Apren més sobre l'estructura de projectes d'Astro en la nostra [Guia d'estructura de projectes](/ca/core-concepts/project-structure/).
