---
title: Sviluppare e costruire
description: 'Come iniziare a lavorare su un nuovo progetto.'
i18nReady: true
---

import { Tabs, TabItem, FileTree, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';
import ReadMore from '~/components/ReadMore.astro';

Una volta che hai un progetto Astro, sei pronto per iniziare a costruire con Astro! 🚀

## Modifica il tuo progetto

Per fare modifiche al tuo progetto, apri la cartella del progetto nel tuo editor di codice. Lavorare in modalità sviluppo con il server di sviluppo in esecuzione ti permette di vedere gli aggiornamenti del sito mentre modifichi il codice.

Puoi anche [personalizzare aspetti del tuo ambiente di sviluppo](#configura-il-tuo-ambiente-di-sviluppo) come configurare TypeScript o installare le estensioni ufficiali di Astro per l'editor.

### Avvia il server di sviluppo di Astro

Astro viene fornito con un server di sviluppo integrato che ha tutto ciò che ti serve per lo sviluppo del progetto. Il comando CLI `astro dev` avvia il server di sviluppo locale in modo che tu possa vedere il tuo nuovo sito in azione per la prima volta.

Ogni template di avvio viene con uno script pre-configurato che eseguirà `astro dev` per te. Dopo essere entrato nella directory del progetto, usa il tuo package manager preferito per eseguire questo comando e avviare il server di sviluppo di Astro.

<Tabs>
  <TabItem label="npm">
  ```shell
  npm run dev
  ```
  </TabItem>
  <TabItem label="pnpm">
  ```shell
  pnpm run dev
  ```
  </TabItem>
  <TabItem label="yarn">
  ```shell
  yarn run dev
  ```
  </TabItem>
</Tabs>

Se tutto va bene, Astro servirà il tuo progetto su [http://localhost:4321/](http://localhost:4321/). Visita quel link nel tuo browser e vedrai il tuo nuovo sito!

### Lavorare in modalità sviluppo

Astro ascolta i cambiamenti nei file della tua cartella `src/` e aggiorna l'anteprima del tuo sito mentre costruisci, quindi non sarà necessario riavviare il server mentre fai modifiche durante lo sviluppo. Potrai sempre vedere una versione aggiornata del tuo sito nel browser quando il server di sviluppo è in esecuzione.

Quando visualizzi il tuo sito nel browser, avrai accesso alla [barra degli strumenti di sviluppo di Astro](/it/guides/dev-toolbar/). Mentre costruisci, ti aiuterà a ispezionare le tue [isole](/it/concepts/islands/), individuare problemi di accessibilità e altro ancora.

Se non riesci ad aprire il tuo progetto nel browser dopo aver avviato il server di sviluppo, torna al terminale dove hai eseguito il comando `dev` e controlla il messaggio visualizzato. Dovrebbe dirti se è stato rilevato un errore o se il tuo progetto viene servito su un URL diverso da [http://localhost:4321/](http://localhost:4321/).

## Costruire e visualizzare il tuo sito

Per controllare la versione del tuo sito che verrà creata al momento della build, esci dal server di sviluppo (<kbd>Ctrl</kbd> + <kbd>C</kbd>) ed esegui il comando di build appropriato per il tuo package manager nel terminale:

<Tabs>
  <TabItem label="npm">
  ```shell
  npm run build
  ```
  </TabItem>
  <TabItem label="pnpm">
  ```shell
  pnpm build
  ```
  </TabItem>
  <TabItem label="yarn">
  ```shell
  yarn run build
  ```
  </TabItem>
</Tabs>

Astro costruirà una versione del sito pronta per la distribuzione in una cartella separata (`dist/` per default) e potrai seguire i progressi nel terminale. Questo ti avviserà di eventuali errori di build nel tuo progetto prima di distribuirlo in produzione. Se TypeScript è configurato su `strict` o `strictest`, lo script `build` controllerà anche eventuali errori di tipo nel tuo progetto.

Quando la build è terminata, esegui il comando `preview` appropriato (ad esempio, `npm run preview`) nel terminale e potrai visualizzare la versione costruita del tuo sito localmente nella stessa finestra di anteprima del browser.

Nota che questo visualizzerà il tuo codice così com'era al momento in cui è stato eseguito l'ultimo comando di build. Questo serve per darti un'anteprima di come il tuo sito apparirà quando verrà distribuito sul web. Eventuali modifiche che farai al codice dopo la build **non** saranno riflettute nell'anteprima finché non eseguirai di nuovo il comando di build.

Usa (<kbd>Ctrl</kbd> + <kbd>C</kbd>) per uscire dall'anteprima e eseguire un altro comando nel terminale, come ad esempio riavviare il server di sviluppo per tornare a [lavorare in modalità sviluppo](#lavorare-in-modalità-sviluppo), che si aggiorna automaticamente mentre modifichi il codice per mostrare un'anteprima in tempo reale delle modifiche.

<ReadMore>Leggi di più su [l'Astro CLI](/it/reference/cli-reference/) e sui comandi del terminale che utilizzerai mentre costruisci con Astro.</ReadMore>

:::tip
Potresti voler [distribuire il tuo nuovo sito subito](/it/guides/deploy/), prima di iniziare ad aggiungere o modificare troppo codice. Questo è utile per ottenere una versione minima e funzionante del tuo sito pubblicata e può farti risparmiare tempo ed energie nel risolvere eventuali problemi di distribuzione in seguito.
:::

## Passi successivi

Successo! Ora sei pronto per iniziare a costruire con Astro! 🥳

Ecco alcune cose che ti consigliamo di esplorare successivamente. Puoi leggerle in qualsiasi ordine. Puoi anche lasciare temporaneamente la nostra documentazione e andare a giocare con il codice del tuo nuovo progetto Astro, tornando qui ogni volta che hai bisogno di aiuto o hai domande.

### Configura il tuo ambiente di sviluppo

Esplora le guide qui sotto per personalizzare la tua esperienza di sviluppo.

<CardGrid>
  <LinkCard
    title="Impostazioni editor"
    description="Personalizza il tuo editor di codice per migliorare l'esperienza di sviluppo con Astro e sbloccare nuove funzionalità."
    href="/it/editor-setup/"
  />
  <LinkCard
    title="Dev Toolbar"
    description="Esplora le funzionalità utili della barra degli strumenti di sviluppo."
    href="/it/guides/dev-toolbar/"
  />
  <LinkCard
    title="Configurazione TypeScript"
    description="Configura le opzioni per il controllo dei tipi, IntelliSense e altro."
    href="/it/guides/typescript/"
  />
</CardGrid>

### Esplora le funzionalità di Astro

<CardGrid>
  <LinkCard
    title="Comprendere la tua base di codice"
    description="Impara la struttura dei file di Astro nella nostra guida alla Struttura del Progetto."
    href="/it/basics/project-structure/"
  />
  <LinkCard
    title="Crea raccolte di contenuti"
    description="Aggiungi contenuti al tuo nuovo sito con validazione frontmatter e sicurezza automatica dei tipi."
    href="/it/guides/content-collections/"
  />
  <LinkCard
    title="Aggiungi transizioni di vista"
    description="Crea transizioni e animazioni fluide tra le pagine."
    href="/it/guides/view-transitions/"
  />
  <LinkCard
    title="Impara le Isole"
    description="Scopri l'architettura delle isole di Astro."
    href="/it/concepts/islands/"
  />
</CardGrid>

### Fai il tutorial introduttivo

Costruisci un blog Astro completamente funzionale a partire da una pagina vuota nel nostro [tutorial introduttivo](/it/tutorial/0-introduction/).

È un ottimo modo per vedere come funziona Astro e ti guida attraverso le basi delle pagine, layout, componenti, routing, isole e altro. Include anche un'unità opzionale, adatta ai principianti, per chi è nuovo nei concetti di sviluppo web in generale, che ti guiderà nell'installazione delle applicazioni necessarie sul tuo computer, nella creazione di un account GitHub e nella distribuzione del tuo sito.


