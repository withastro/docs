---
type: tutorial
title: Crea il tuo primo progetto Astro
description: |-
  Tutorial: Crea il tuo primo blog Astro —
  Crea un nuovo progetto per il tutorial e preparati a programmare
---
import Badge from '~/components/Badge.astro';
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import InstallGuideTabGroup from '~/components/TabGroup/InstallGuideTabGroup.astro';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';



<PreCheck>
  - Esegui l'installazione guidata `create astro` per creare un nuovo progetto Astro
  - Avvia il server Astro in modalità sviluppo (dev)
  - Visualizza una preview in tempo reale del tuo sito nel tuo browser
</PreCheck>

## Esegui l'installazione guidata di Astro

Il metodo preferito per creare un nuovo sito Astro è tramite la nostra installazione guidata `create astro`.

1. Nel prompt dei comandi del tuo terminale, esegui il comando seguente usando il tuo package manager preferito:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      # crea un nuovo progetto con npm
      npm create astro@latest
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      # crea un nuovo progetto con pnpm
      pnpm create astro@latest
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      # crea un nuovo progetto con yarn
      yarn create astro
      ```
      </Fragment>
    </PackageManagerTabs>

2. Conferma `y` per installare `create-astro`
3. Quando il prompt chiede, "Where would you like to create your new project?" inserisci il nome della cartella per creare una nuova directory per il tuo progetto, per esempio `./tutorial`

    :::note
    Un nuovo progetto Astro può solo essere creato in una cartella completamente vuota, quindi scegli un nome per la tua cartella che non esista già!
    :::

4. Vedrai una breve lista di template con cui iniziare. Usa le freccie (su e giù) per navigare al template "Empty", e poi premi invio (enter) per inviare la tua scelta.

5. Quando il prompt chiede "Would you like to install dependencies?", inserisci `y`.

6. Quando il prompt chiede se vuoi utilizzare TypeScript, inserisci `n`.

7. Quando il prompt chiede "Would you like to initialize a new git repository?", inserisci `y`.

Quando hai completato l'installazione guidata, non hai più bisogno di questo terminale. Ora puoi aprire VS Code per continuare.

## Apri il tuo progetto in VS Code

1. Apri VS Code. Ti verrà richiesto di aprire una cartella. Seleziona la cartella che hai creato durante l'installazione guidata.

2. Se questa è la prima volta che apri un progetto Astro, dovresti vedere una notifica che ti chiede se vuoi installare le estensioni consigliate. Clicca per vedere le estensioni consigliate, e scegli la "[Astro language support extension](https://marketplace.visualstudio.com/items?itemName=astro-build.astro-vscode)". Questa fornirà evidenziazione della sintassi e completamento automatico del codice Astro.

3. Assicurati che il terminale sia visibile e sia presente il prompt dei comandi, per esempio:

    ```sh
    user@machine:~/tutorial$
    ```

    :::tip[Scorciatoia da tastiera]
    Per alternare la visibilità del terminal, usa <kbd>Ctrl + J</kbd> (macOS: <kbd>Cmd ⌘ + J</kbd>).
    :::

Ora puoi utilizzare il terminale integrato in questa finestra, invece del terminale del tuo computer, per il resto di questo tutorial.



## Esegui Astro in modalità sviluppo (dev)

In modo di avere una preview dei file del tuo progetto _come un sito_ mentre lavori, avrai bisogno che Astro sia eseguito in modalità sviluppo (dev).

### Avvia il server dev

1. Esegui il comando per avviare il server Astro dev scrivendo nel terminale di VS Code:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      npm run dev
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      pnpm run dev
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      yarn run dev
      ```
      </Fragment>
    </PackageManagerTabs>

    Ora dovresti avere conferma sul terminale che Astro è in esecuzione in modalità dev. 🚀

## Visualizza un'anteprima del tuo sito

I file del tuo progetto contengono tutto il codice necessario per mostrare un sito Astro, ma il browser è responsabile per mostrare il tuo codice come pagine web.

1. Clicca sul link `localhost` sulla finestra del tuo terminale per visualizzare un'anteprima del tuo nuovo sito Astro!

    (Astro usa `http://localhost:4322` di default se la porta `4321` è disponibile.)

    Ecco come il sito iniziale di Astro "Empty Project" dovrebbe apparire nell'anteprima del browser:

    ![A blank white page with the word Astro at the top.](/tutorial/minimal.png)

:::tip[Utilizzare il server in modalità di sviluppo di Astro]

Mentre il server Astro è in esecuzione in modalità di sviluppo, NON potrai eseguire dei comandi nella tua finestra del terminale. Invece, questo riquadro ti darà feedback mentre visualizzi l'anteprima del tuo sito.

Puoi fermare il server dev in qualunque momento e ritornare al prompt dei comandi inserendo <kbd>Ctrl + C</kbd> nel terminale.

Alcune volte il server in modalità di sviluppo si fermerà da solo mentre stai lavorando. Se l'anteprima in tempo reale smette di funzionare, ritorna al terminale e riavvia il server dev inserendo `npm run dev`.
:::

<Box icon="check-list">
## Checklist

<Checklist>
- [ ] So creare un nuovo progetto Astro
- [ ] So avviare il server dev Astro.
</Checklist>
</Box>

### Risorse

- <p>[Getting Started with Visual Studio Code](https://code.visualstudio.com/docs/introvideos/basics) <Badge>esterno</Badge> — un video tutorial per installare, impostare e lavorare con VS Code</p>
