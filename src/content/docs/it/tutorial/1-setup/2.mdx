---
type: tutorial
title: Crea il tuo primo progetto Astro
description: |-
  Tutorial: Costruisci il tuo primo blog con Astro —
  Crea un nuovo progetto per il tutorial Astro e preparati a scrivere codice
i18nReady: true
---
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import InstallGuideTabGroup from '~/components/TabGroup/InstallGuideTabGroup.astro';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';
import { Steps } from '@astrojs/starlight/components';
import Badge from "~/components/Badge.astro"

<PreCheck>
  - Esegui la procedura guidata `create astro` per creare un nuovo progetto Astro
  - Avvia il server Astro in modalità sviluppo (dev)
  - Visualizza un'anteprima live del tuo sito nel browser
</PreCheck>

## Avvia la procedura guidata di configurazione di Astro

Il metodo preferito per creare un nuovo sito Astro è attraverso la procedura guidata di configurazione `create astro`.

<Steps>
1. Nella riga di comando del tuo terminale, esegui il seguente comando utilizzando il tuo gestore di pacchetti preferito:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      # crea un nuovo progetto con npm
      npm create astro@latest -- --template minimal
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      # crea un nuovo progetto con pnpm
      pnpm create astro@latest --template minimal
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      # crea un nuovo progetto con yarn
      yarn create astro --template minimal
      ```
      </Fragment>
    </PackageManagerTabs>

2. Conferma con `y` per installare `create-astro`
3. Quando la procedura guidata ti chiede dove creare il progetto, digita il nome di una cartella per creare una nuova directory per il tuo progetto, ad esempio:
`./tutorial`

    :::note
    Un nuovo progetto Astro può essere creato solo in una cartella completamente vuota, quindi scegli un nome per la cartella che non esista già!
    :::

4. Quando la procedura guidata ti chiede se installare le dipendenze, digita o seleziona `y`.

5. Quando la procedura guidata ti chiede se inizializzare un nuovo repository git, digita o seleziona `y`.
</Steps>

Quando la procedura di installazione è completa, non avrai più bisogno di questo terminale. Ora puoi aprire VS Code per continuare.

## Apri il tuo progetto in VS Code

<Steps>
6. Apri VS Code. Ti verrà chiesto di aprire una cartella. Scegli la cartella che hai creato durante la procedura guidata.

7. Se è la prima volta che apri un progetto Astro, dovresti vedere una notifica che ti chiede se desideri installare le estensioni consigliate. Clicca per vedere le estensioni consigliate e scegli l'[estensione di supporto linguistico Astro](https://marketplace.visualstudio.com/items?itemName=astro-build.astro-vscode). Questa fornirà evidenziazione della sintassi e autocompletamento per il tuo codice Astro.

8. Assicurati che il terminale sia visibile e che tu possa vedere il prompt dei comandi, ad esempio:

    ```sh
    user@machine:~/tutorial$
    ```

    :::tip[Scorciatoia da tastiera]
    Per alternare la visibilità del terminale, usa <kbd>Ctrl + J</kbd> (macOS: <kbd>Cmd ⌘ + J</kbd>).
    :::
</Steps>

Ora puoi usare il terminale integrato in questa finestra, invece di usare l'app Terminale del tuo computer, per il resto di questo tutorial.



## Esegui Astro in modalità sviluppo

Per visualizzare i file del tuo progetto _come sito web_ mentre lavori, dovrai far funzionare Astro in modalità sviluppo (dev).

### Avvia il server di sviluppo

<Steps>
9. Esegui il comando per avviare il server di sviluppo Astro digitando nel terminale di VS Code:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      npm run dev
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      pnpm run dev
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      yarn run dev
      ```
      </Fragment>
    </PackageManagerTabs>

    Ora dovresti vedere una conferma nel terminale che Astro è in esecuzione in modalità sviluppo. 🚀
</Steps>

## Visualizza un'anteprima del tuo sito

I file del tuo progetto contengono tutto il codice necessario per visualizzare un sito Astro, ma il browser è responsabile di visualizzare il tuo codice come pagine web.

<Steps>
10. Clicca sul link `localhost` nella finestra del terminale per vedere un'anteprima live del tuo nuovo sito Astro! 

    (Astro usa `http://localhost:4321` per impostazione predefinita se la porta `4321` è disponibile.)

    Ecco come dovrebbe apparire il sito starter "Empty Project" di Astro nell'anteprima del browser:

    ![Una pagina bianca con la parola Astro in alto.](/tutorial/minimal.png)

</Steps>

:::tip[Usare il server di sviluppo Astro]

Mentre il server di Astro è in esecuzione in modalità sviluppo, NON potrai eseguire comandi nella finestra del terminale. Invece, questo pannello ti fornirà feedback mentre visualizzi il tuo sito.

Puoi fermare il server di sviluppo in qualsiasi momento e tornare al prompt dei comandi digitando <kbd>Ctrl + C</kbd> nel terminale.

A volte, il server di sviluppo si ferma da solo mentre lavori. Se la tua anteprima live smette di funzionare, torna al terminale e riavvia il server di sviluppo digitando `npm run dev`.
:::

<Box icon="check-list">
## Lista di controllo

<Checklist>
- [ ] Posso creare un nuovo progetto Astro.
- [ ] Posso avviare il server di sviluppo Astro.
</Checklist>
</Box>

### Risorse

- <p>[Getting Started with Visual Studio Code](https://code.visualstudio.com/docs/introvideos/basics) <Badge class="neutral-badge" text="esterno" /> — un tutorial video per installare, configurare e lavorare con VS Code</p>
