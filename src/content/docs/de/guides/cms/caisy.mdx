---
title: Caisy & Astro
description: Füge deinem Astro-Projekt Inhalte hinzu, indem du caisy als CMS verwendest
type: cms
service: Caisy
i18nReady: true
---
[Caisy](https://caisy.io/) ist ein Headless-Content-Management-System mit zusätzlichen Funktionen für Multi-Tenancy, das alle Funktionen über eine GraphQL-API und eine ausgezeichnete Benutzeroberfläche bereitstellt.

## Vollständiges Beispielprojekt mit caisy und Astro:
*   Github: [https://github.com/caisy-io/caisy-example-astro](https://github.com/caisy-io/caisy-example-astro)
*   Stackblitz: [https://stackblitz.com/github/caisy-io/caisy-example-astro](https://stackblitz.com/github/caisy-io/caisy-example-astro?file=src%2Fpages%2Fblog%2F[...slug].astro)

## Beispiel
Einfaches Datenabrufbeispiel mit graphql-request

```astro title="src/pages/blog/[...slug].astro"
---
import { gql, GraphQLClient } from 'graphql-request';

const params = Astro.params;

const client = new GraphQLClient(
	`https://cloud.caisy.io/api/v3/e/${import.meta.env.CAISY_PROJECT_ID}/graphql`,
	{
		headers: {
			'x-caisy-apikey': import.meta.env.CAISY_API_KEY
		}
	}
);

const gqlResponse = await client.request(
	gql`
		query allBlogArticle($slug: String) {
			allBlogArticle(where: { slug: { eq: $slug } }) {
				edges {
					node {
						text {
							json
						}
						title
						slug
						id
					}
				}
			}
		}
	`,
	{ slug: params.slug }
);

const post = gqlResponse?.allBlogArticle?.edges?.[0]?.node;
---
<h1>{post.title}</h1>
```

## Weiterlesen
Vertiefe dein Wissen über fortgeschrittene Themen wie die externe caisy GraphQL API. 
*  Abfrage deiner Dokumente im [Entwurfsmodus](https://caisy.io/developer/docs/external-api/localization-and-preview#preview-mode-15) und in mehreren [Sprachen](https://caisy.io/developer/docs/external-api/localization-and-preview#localization-in-a-graphql-query-8).
*  Verwende [Seitennummerierung](https://caisy.io/developer/docs/external-api/queries-pagination) für umfangreiche Dokumentanfragen.
*  Nutze [Filter](https://caisy.io/developer/docs/external-api/external-filter-and-sorting) in deinen Abfragen und [sortiere](https://caisy.io/developer/docs/external-api/external-filter-and-sorting#sorting-8) die Ergebnisse.
