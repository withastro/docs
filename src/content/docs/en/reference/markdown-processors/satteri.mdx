---
title: Sätteri API Reference
sidebar:
  label: 'Sätteri'
i18nReady: true
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 3
---
import Since from '~/components/Since.astro';
import ReadMore from '~/components/ReadMore.astro';

<p><Since v="6.4.0" /></p>

## Imports from `@astrojs/markdown-satteri`

The following helpers are imported from `@astrojs/markdown-satteri`:

```ts
import {
  createSatteriMarkdownProcessor,
  isSatteriProcessor,
  satteri,
  satteriCollectHastText,
  satteriCollectImagesPlugin,
  satteriCreateHighlightFn,
  satteriHeadingIdsPlugin,
  satteriHighlightPlugin,
  satteriImageMarkerPlugin,
} from "@astrojs/markdown-satteri";
```

## `@astrojs/markdown-satteri` types

The following types are imported from `@astrojs/markdown-satteri`:

```ts
import type {
  SatteriAstroData,
  SatteriFeatures,
  SatteriMarkdownProcessorOptions,
  SatteriProcessorOptions,
  SatteriResolvedOptions,
} from "@astrojs/markdown-satteri";
```
