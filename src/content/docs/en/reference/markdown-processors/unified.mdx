---
title: Unified API Reference
sidebar:
  label: 'Unified'
i18nReady: true
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 3
---
import Since from '~/components/Since.astro';
import ReadMore from '~/components/ReadMore.astro';

<p><Since v="6.4.0" /></p>

## Imports from `@astrojs/markdown-remark`

The following helpers are imported from `@astrojs/markdown-remark`:

```ts
import {
  createMarkdownProcessor,
  isUnifiedProcessor,
  parseFrontmatter,
  rehypeHeadingIds,
  unified,
} from "@astrojs/markdown-remark";
```

### `createMarkdownProcessor()`

<p>

**Type:** <code>(opts?: <a href="/en/reference/configuration-reference/#markdown-options">AstroMarkdownOptions</a>) => Promise\<<a href="#markdownrenderer">MarkdownRenderer</a>\></code>
</p>

Creates a standalone Unified-based Markdown renderer. This is useful when you need to render Markdown independently of a project's [configured `markdown.processor`](/en/reference/configuration-reference/#markdownprocessor).

```ts title="src/utils/markdown.ts"
import { createMarkdownProcessor } from "@astrojs/markdown-remark";

const processor = await createMarkdownProcessor();
const { code } = await processor.render("# Hello world");
```

### `isUnifiedProcessor()`

<p>

**Type:** `(p: { name: string; }) => boolean`
</p>

Determines whether the given processor is a Unified Markdown processor. This is useful when you need to check the type of a processor before applying Unified-specific logic.

The following example checks, within an integration, whether the configured Markdown processor is a Unified processor:

```ts
import { isUnifiedProcessor } from "@astrojs/markdown-remark";

export default function myIntegration() {
  return {
    name: "my-integration",
    hooks: {
      "astro:config:done": ({ config }) => {
        if (isUnifiedProcessor(config.markdown.processor)) {
          // Apply Unified-specific logic here
        }
      },
    },
  };
}
```

### `rehypeHeadingIds()`

<p>

**Type:** <code><a href="#rehypeplugin">RehypePlugin</a>\<[]\></code>
</p>

Generates and collects heading IDs for all headings in the Markdown content. This makes them [available when importing or rendering Markdown content](/en/guides/markdown-content/#available-properties).

<ReadMore>See [heading IDs and plugins](/en/guides/markdown-content/#heading-ids-and-plugins) in the Markdown guide for usage example.</ReadMore>

### `unified()`

<p>

**Type:** <code>(opts?: <a href="#unifiedprocessoroptions">UnifiedProcessorOptions</a>) => <a href="#markdownprocessor">MarkdownProcessor</a>\<<a href="#unifiedresolvedoptions">UnifiedResolvedOptions</a>\></code>
</p>

Creates a Unified-based Markdown processor to render `.md` and `.mdx` files. Pass options to customize the processor's behavior, such as [adding remark or rehype plugins](/en/guides/markdown-content/#markdown-processor-plugins).

## `@astrojs/markdown-remark` types

The following types are imported from `@astrojs/markdown-remark`:

```ts
import type {
  AstroMarkdownOptions,
  MarkdownHeading,
  MarkdownProcessor,
  MarkdownRenderOptions,
  MarkdownRenderResult,
  MarkdownRenderer,
  RehypePlugin,
  RehypePlugins,
  RemarkPlugin,
  RemarkPlugins,
  RemarkRehype,
  ShikiConfig,
  Smartypants,
  SyntaxHighlightConfig,
  SyntaxHighlightConfigType,
  UnifiedProcessorOptions,
  UnifiedResolvedOptions,
} from "@astrojs/markdown-remark";
```

### `AstroMarkdownOptions`

<p>

**Type:** <code>\{ syntaxHighlight?: <a href="#syntaxhighlightconfig">SyntaxHighlightConfig</a> | <a href="#syntaxhighlightconfigtype">SyntaxHighlightConfigType</a> | false; shikiConfig?: <a href="#shikiconfig">ShikiConfig</a>; image?: \{ domains?: string[]; remotePatterns?: RemotePattern[]; \}; gfm?: boolean; smartypants?: boolean | <a href="#smartypants">Smartypants</a>; remarkPlugins?: <a href="#remarkplugins">RemarkPlugins</a>; rehypePlugins?: <a href="#rehypeplugins">RehypePlugins</a>; remarkRehype?: <a href="#remarkrehype">RemarkRehype</a>; \}</code>
</p>

Describes the options available for configuring the Unified-based Markdown processor.

#### `AstroMarkdownOptions.syntaxHighlight`

<p>

**Type:** <code><a href="#syntaxhighlightconfig">SyntaxHighlightConfig</a> | <a href="#syntaxhighlightconfigtype">SyntaxHighlightConfigType</a> | false</code>
</p>

Specifies the syntax highlighting configuration for the Markdown processor. Set to `false` to disable syntax highlighting.

#### `AstroMarkdownOptions.shikiConfig`

<p>

**Type:** <code><a href="#shikiconfig">ShikiConfig</a></code>
</p>

Specifies the Shiki configuration for the Markdown processor.

#### `AstroMarkdownOptions.image`

<p>

**Type:** <code>\{ domains?: string[]; remotePatterns?: <a href="/en/reference/modules/astro-assets/#remotepattern">RemotePattern[]</a>; \}</code>
</p>

Specifies the image configuration for the Markdown processor, including allowed domains and remote patterns.

#### `AstroMarkdownOptions.gfm`

<p>

**Type:** `boolean`
</p>

Toggles GitHub Flavored Markdown (GFM) support in the Markdown processor.

#### `AstroMarkdownOptions.smartypants`

<p>

**Type:** <code>boolean | <a href="#smartypants">Smartypants</a></code>
</p>

Controls smart punctuation transformations in the Markdown processor. Set to `false` to disable smart punctuation, or provide a configuration object to customize its behavior.

#### `AstroMarkdownOptions.remarkPlugins`

<p>

**Type:** <code><a href="#remarkplugins">RemarkPlugins</a></code>
</p>

Specifies the Remark plugins to use with the Markdown processor.

#### `AstroMarkdownOptions.rehypePlugins`

<p>

**Type:** <code><a href="#rehypeplugins">RehypePlugins</a></code>
</p>

Specifies the Rehype plugins to use with the Markdown processor.

#### `AstroMarkdownOptions.remarkRehype`

<p>

**Type:** <code><a href="#remarkrehype">RemarkRehype</a></code>
</p>

Specifies the [`remark-rehype` configuration](https://github.com/remarkjs/remark-rehype#options) to use with the Markdown processor.

### `MarkdownHeading`

<p>

**Type:** `{ depth: number; slug: string; text: string; }`
</p>

Represents a heading in a Markdown or MDX document.

#### `MarkdownHeading.depth`

<p>

**Type:** `number`
</p>

Specifies the heading level, from `1` (`<h1>`) to `6` (`<h6>`).

#### `MarkdownHeading.slug`

<p>

**Type:** `string`
</p>

Specifies the generated slug used as the `id` attribute for anchor links.

#### `MarkdownHeading.text`

<p>

**Type:** `string`
</p>

Specifies the heading's rendered text content.

### `MarkdownProcessor`

<p>

**Type:** `object`
</p>

Describes the interface implemented by a Markdown processor. This is useful when you want to indicate that a variable accepts any [built-in Markdown processor](/en/guides/markdown-content/#markdown-processors).

### `MarkdownRenderOptions`

<p>

**Type:** `{ fileURL?: URL; frontmatter?: Record<string, any>; }`
</p>

Specifies the options available when rendering Markdown content.

#### `MarkdownRenderOptions.fileURL`

<p>

**Type:** `URL`
</p>

Specifies the URL of the Markdown file being rendered.

#### `MarkdownRenderOptions.frontmatter`

<p>

**Type:** `Record<string, any>`
</p>

Specifies the frontmatter data extracted from the Markdown file.

### `MarkdownRenderResult`

<p>

**Type:** <code>\{ code: string; metadata: \{ headings: <a href="#markdownheading">MarkdownHeading</a>[]; localImagePaths: string[]; remoteImagePaths: string[]; frontmatter: Record\<string, any\>; \}; \}</code>
</p>

Represents a rendered Markdown file.

#### `MarkdownRenderResult.code`

<p>

**Type:** `string`
</p>

Specifies the rendered HTML code.

#### `MarkdownRenderResult.metadata`

<p>

**Type:** <code>\{ headings: <a href="#markdownheading">MarkdownHeading</a>[]; localImagePaths: string[]; remoteImagePaths: string[]; frontmatter: Record\<string, any\>; \}</code>
</p>

Specifies the metadata associated with the rendered content.

##### `MarkdownRenderResult.metadata.headings`

<p>

**Type:** <code><a href="#markdownheading">MarkdownHeading</a>[]</code>
</p>

Specifies the headings extracted from the rendered content.

##### `MarkdownRenderResult.metadata.localImagePaths`

<p>

**Type:** `string[]`
</p>

Specifies the local image paths referenced in the rendered content.

##### `MarkdownRenderResult.metadata.remoteImagePaths`

<p>

**Type:** `string[]`
</p>

Specifies the remote image paths referenced in the rendered content.

##### `MarkdownRenderResult.metadata.frontmatter`

<p>

**Type:** `Record<string, any>`
</p>

Specifies the frontmatter data extracted from the rendered content.

### `MarkdownRenderer`

<p>

**Type:** <code>\{ render: (content: string, opts?: <a href="#markdownrenderoptions">MarkdownRenderOptions</a>) => Promise\<<a href="#markdownrenderresult">MarkdownRenderResult</a>\>; \}</code>
</p>

Specifies the interface implemented by a Markdown renderer.

### `RehypePlugin`

<p>

**Type:** `Plugin<PluginParameters, Root>`
</p>

The type used for [rehype plugins](https://github.com/rehypejs/rehype) in the unified ecosystem.

### `RehypePlugins`

<p>

**Type:** <code>(string | [string, any] | <a href="#rehypeplugin">RehypePlugin</a> | [<a href="#rehypeplugin">RehypePlugin</a>, any])[]</code>
</p>

The `rehypePlugins` accepted by the [`unified()`](#unified) processor. Each entry is a rehype plugin, or its package name as a string for Astro to import automatically.

### `RemarkPlugin`

<p>

**Type:** `Plugin<PluginParameters, Root>`
</p>

The type used for [remark plugins](https://github.com/remarkjs/remark) in the unified ecosystem.

### `RemarkPlugins`

<p>

**Type:** <code>(string | [string, any] | <a href="#remarkplugin">RemarkPlugin</a> | [<a href="#remarkplugin">RemarkPlugin</a>, any])[]</code>
</p>

The `remarkPlugins` accepted by the [`unified()`](#unified) processor. Each entry is a remark plugin, or its package name as a string for Astro to import automatically.

### `RemarkRehype`

<p>

**Type:** `Record<string, unknown>`
</p>

Specifies the options passed to the [`remark-rehype`](https://github.com/remarkjs/remark-rehype) plugin.

### `ShikiConfig`

<p>

**Type:** `object`
</p>

Specifies the configuration options for the [Shiki syntax highlighter](/en/guides/syntax-highlighting/).

<ReadMore>See [`markdown.shikiConfig`](/en/reference/configuration-reference/#markdownshikiconfig) for the full reference of supported properties.</ReadMore>

### `Smartypants`

<p>

**Type:** `object`
</p>

Specifies the [configuration options for `retext-smartypants`](https://github.com/retextjs/retext-smartypants#options).

<ReadMore>Learn more about using [smart punctuation](/en/guides/markdown-content/#smart-punctuation) in Astro.</ReadMore>

### `SyntaxHighlightConfig`

<p>

**Type:** <code>\{ type: <a href="#syntaxhighlightconfigtype">SyntaxHighlightConfigType</a>; excludeLangs?: string[]; \}</code>
</p>

Specifies the configuration options for [syntax highlighting](/en/guides/syntax-highlighting/) in Astro.

<ReadMore>See [`markdown.syntaxHighlight`](/en/reference/configuration-reference/#markdownsyntaxhighlight) for the full reference of supported properties.</ReadMore>

### `SyntaxHighlightConfigType`

<p>

**Type:** `'shiki' | 'prism'`
</p>

A union of supported syntax highlighter in Astro.

### `UnifiedProcessorOptions`

<p>

**Type:** <code>\{ remarkPlugins?: <a href="#remarkplugins">RemarkPlugins</a>; rehypePlugins?: <a href="#rehypeplugins">RehypePlugins</a>; remarkRehype?: <a href="#remarkrehype">RemarkRehype</a>; recmaPlugins?: PluggableList; gfm?: boolean; smartypants?: boolean | <a href="#smartypants">Smartypants</a>; \}</code>
</p>

Specifies the options that can be passed to the [unified processor](#unified) in Astro.

#### `UnifiedProcessorOptions.remarkPlugins`

<p>

**Type:** [`RemarkPlugins`](#remarkplugins)
</p>

Specifies the remark plugins to be used by the unified processor.

#### `UnifiedProcessorOptions.rehypePlugins`
<p>

**Type:** [`RehypePlugins`](#rehypeplugins)
</p>
Specifies the rehype plugins to be used by the unified processor.

#### `UnifiedProcessorOptions.remarkRehype`

<p>

**Type:** [`RemarkRehype`](#remarkrehype)
</p>

Specifies the options passed to the [ `remark-rehype`](https://github.com/remarkjs/remark-rehype) plugin.

#### `UnifiedProcessorOptions.recmaPlugins`

<p>

**Type:** `PluggableList`
</p>

Specifies the recma plugins to be used by the unified processor when processing [MDX content](/en/guides/integrations-guide/mdx/).

#### `UnifiedProcessorOptions.gfm`

<p>

**Type:** `boolean`
</p>

Specifies whether GitHub Flavored Markdown (GFM) support is enabled in the unified processor.

<ReadMore>Learn more about using [GitHub Flavored Markdown](/en/guides/markdown-content/#github-flavored-markdown) in Astro.</ReadMore>

#### `UnifiedProcessorOptions.smartypants`

<p>

**Type:** <code>boolean | <a href="#smartypants">Smartypants</a></code>
</p>

Specifies the smartypants configuration for the unified processor.

### `UnifiedResolvedOptions`

<p>

**Type:** <code>\{ remarkPlugins: <a href="#remarkplugins">RemarkPlugins</a>; rehypePlugins: <a href="#rehypeplugins">RehypePlugins</a>; remarkRehype: <a href="#remarkrehype">RemarkRehype</a>; recmaPlugins: PluggableList; gfm?: boolean; smartypants?: boolean | <a href="#smartypants">Smartypants</a>; \}</code>
</p>

Describes the options passed to the unified processor after merging user-provided options with the defaults.
