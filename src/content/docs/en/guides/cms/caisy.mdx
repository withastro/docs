---
title: Caisy & Astro
description: Add content to your Astro project using caisy as a CMS
type: cms
service: Caisy
i18nReady: true
---
[Caisy](https://caisy.io/) is a headless content management system with great multi tancy capabities that exposes a GraphQL API and rich user interface for all features.

## Full example project with caisy and Astro:
*   Github: [https://github.com/caisy-io/caisy-example-astro](https://github.com/caisy-io/caisy-example-astro)
*   Stackblitz: [https://stackblitz.com/github/caisy-io/caisy-example-astro](https://stackblitz.com/github/caisy-io/caisy-example-astro?file=src%2Fpages%2Fblog%2F[...slug].astro)
    
## Example
Basic data fetching example with graphql-request

```astro title="src/pages/blog/[...slug].astro"
---
import { gql, GraphQLClient } from 'graphql-request';

const params = Astro.params;

const client = new GraphQLClient(
	`https://cloud.caisy.io/api/v3/e/${import.meta.env.CAISY_PROJECT_ID}/graphql`,
	{
		headers: {
			'x-caisy-apikey': import.meta.env.CAISY_API_KEY
		}
	}
);

const gqlResponse = await client.request(
	gql`
		query allBlogArticle($slug: String) {
			allBlogArticle(where: { slug: { eq: $slug } }) {
				edges {
					node {
						text {
							json
						}
						title
						slug
						id
					}
				}
			}
		}
	`,
	{ slug: params.slug }
);

const post = gqlResponse?.allBlogArticle?.edges?.[0]?.node;
---
<h1>{post.title}</h1>
```

## Continue reading 
Continue reading on more advanced topics reading the caisy external graphql API. 
*  Query your documents in [draft](https://caisy.io/developer/docs/external-api/localization-and-preview#preview-mode-15) mode and multiple [locales](https://caisy.io/developer/docs/external-api/localization-and-preview#localization-in-a-graphql-query-8).
*  Use [pagination](https://caisy.io/developer/docs/external-api/queries-pagination) to query large chunks of documents.
*  Use [filter](https://caisy.io/developer/docs/external-api/external-filter-and-sorting) in your queries and [order](https://caisy.io/developer/docs/external-api/external-filter-and-sorting#sorting-8) the results
