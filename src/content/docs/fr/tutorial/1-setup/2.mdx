---
type: tutorial
title: Créer votre premier projet Astro
description: |-
  Tutoriel: Créez votre premier blog Astro —
  Créez un nouveau projet pour le tutoriel Astro et préparez-vous à coder
i18nReady: true
---
import Badge from '~/components/Badge.astro';
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import InstallGuideTabGroup from '~/components/TabGroup/InstallGuideTabGroup.astro';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';



<PreCheck>
  - Exécutez l'assistant de configuration`create astro` pour créer un nouveau projet Astro.
  - Démarrer le serveur Astro en mode développement (dev)
  - Affichez un aperçu en direct de votre site web dans votre navigateur
</PreCheck>

## Lancer l'assistant d'installation Astro

La meilleure façon de créer un nouveau site Astro est d'utiliser l'assistant de création de site Astro `create astro`.

1. Dans votre terminal, exécutez la commande suivante en utilisant votre gestionnaire de paquets préféré :

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      # Créer un nouveau projet avec npm
      npm create astro@latest
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      # Créer un nouveau projet avec pnpm
      pnpm create astro@latest
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      # Créer un nouveau projet avec  yarn
      yarn create astro
      ```
      </Fragment>
    </PackageManagerTabs>

2. Confirmer avec `y` pour installer `create-astro`
3. À la question, "Where would you like to create your new project?" saisissez le nom d'un dossier pour créer un nouveau répertoire pour votre projet, par exemple `./tutorial`.

    :::note
    Un nouveau projet Astro ne peut être créé que dans un dossier complètement vide, choisissez donc un nom pour votre dossier qui n'existe pas encore !
    :::

4. Vous verrez une courte liste de modèles de départ parmi lesquels vous pourrez choisir. Utilisez les touches fléchées (haut et bas) pour naviguer jusqu'au modèle "Empty", puis appuyez sur la touche Entrée pour valider votre choix. 

5. À la question, "Would you like to install dependencies?" ("Souhaitez-vous installer des dépendances ?") tapez`y`.

6. Lorsque l'invite vous demande si vous prévoyez d'écrire du TypeScript, tapez`n`. 

7. Lorsque l'invite demande, "Would you like to initialize a new git repository?" ("Souhaitez-vous initialiser un nouveau dépôt git ?",) tapez `y`.

Lorsque l'assistant d'installation est terminé, vous n'avez plus besoin de ce terminal. Vous pouvez maintenant ouvrir VS Code pour continuer.

## Ouvrir votre projet dans VS Code

1. Ouvrir VS Code. Vous serez invité à ouvrir un dossier. Choisissez le dossier que vous avez créé pendant l'assistant d'installation.

2. Si vous ouvrez un projet Astro pour la première fois, vous devriez voir une notification vous demandant si vous souhaitez installer les extensions recommandées. Cliquez pour voir les extensions recommandées, et choisissez l'extension [Astro language support extension](https://marketplace.visualstudio.com/items?itemName=astro-build.astro-vscode). Cette extension fournira la coloration syntaxique et les autocomplétions pour votre code Astro.

3. Assurez-vous que le terminal est visible et que vous pouvez voir l'invite de commande, comme par exemple :

    ```sh
    user@machine:~/tutorial$
    ```

    :::tip[Raccourci Clavier]
    Pour basculer la visibilité du terminal, utilisez <kbd>Ctrl + J</kbd> (macOS: <kbd>Cmd ⌘ + J</kbd>).
    :::

Vous pouvez maintenant utiliser le terminal intégré dans cette fenêtre, au lieu de l'application Terminal de votre ordinateur, pour la suite de ce tutoriel.



## Lancer Astro en mode Dev

Pour pouvoir prévisualiser vos fichiers de projet en tant que site web pendant que vous travaillez, Astro doit fonctionner en mode développement (dev).

### Lancer le serveur de dev

1. Exécutez la commande pour démarrer le serveur Astro dev en tapant dans le terminal de VS Code :

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      npm run dev
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      pnpm run dev
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      yarn run dev
      ```
      </Fragment>
    </PackageManagerTabs>

    Vous devriez maintenant voir la confirmation dans le terminal qu'Astro fonctionne en mode dev.🚀

## Visualisez un aperçu de votre site web

Vos fichiers de projet contiennent tout le code nécessaire pour afficher un site web Astro, mais le navigateur est responsable de l'affichage de votre code sous forme de pages web.

1. Cliquez sur le lien `localhost` dans la fenêtre de votre terminal pour voir un aperçu en direct de votre nouveau site web Astro !

    (Astro uilise `http://localhost:4321` par défaut si le port `4321` est disponible.)

    Voici à quoi devrait ressembler le site Astro "Empty Project" dans l'aperçu du navigateur :

    ![Une page blanche vierge avec le mot Astro en haut.](/tutorial/minimal.png)

:::tip[Utilisation du serveur de développement Astro]

Lorsque le serveur Astro fonctionne en mode développement, vous ne pourrez PAS exécuter de commandes dans votre fenêtre de terminal. Au lieu de cela, ce volet vous donnera un retour d'information lorsque vous prévisualiserez votre site.

Vous pouvez arrêter le serveur de développement à tout moment et revenir à l'invite de commande en tapant <kbd>Ctrl + C</kbd> dans le terminal.

Parfois, le serveur de développement s'arrête de lui-même pendant que vous travaillez. Si votre prévisualisation en direct cesse de fonctionner, retournez dans le terminal et redémarrez le serveur de développement en tapant `npm run dev`.
:::

<Box icon="check-list">
## Checklist

<Checklist>
- [ ] Je peux créer un nouveau projet Astro.
- [ ] Je peux démarrer le serveur Dev d'Astro.
</Checklist>
</Box>

### Resources

- <p>[Premiers pas avec Visual Studio Code (EN)](https://code.visualstudio.com/docs/introvideos/basics) <Badge>externe</Badge> — un tutoriel vidéo pour installer, configurer et travailler avec VS Code</p>
