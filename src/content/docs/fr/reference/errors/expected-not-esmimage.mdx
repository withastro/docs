---
title: Options d'image attendues, pas une image importée par ESM.
i18nReady: true
githubURL: https://github.com/withastro/astro/blob/main/packages/astro/src/core/errors/errors-data.ts
---

> **ExpectedNotESMImage**: An ESM-imported image cannot be passed directly to `getImage()`. Instead, pass an object with the image in the `src` property.

## Qu'est-ce qui a mal tourné ?
Une image importée par ESM ne peut pas être passée directement à `getImage()`. A la place, il faut passer un objet avec l'image dans la propriété `src`.

```diff
import { getImage } from "astro:assets";
import myImage from "../assets/my_image.png";
- const optimizedImage = await getImage( myImage );
+ const optimizedImage = await getImage({ src: myImage });
```

**Voir aussi :**
-  [Images](/fr/guides/images/)


