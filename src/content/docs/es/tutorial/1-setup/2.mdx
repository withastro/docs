---
type: tutorial
title: Cree su primer proyecto Astro
i18nReady: true
description: |-
  Tutorial: Crea tu primer blog Astro —
  Crea un nuevo proyecto para el tutorial de Astro y prepárate para codificar
---
import Badge from '~/components/Badge.astro';
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import InstallGuideTabGroup from '~/components/TabGroup/InstallGuideTabGroup.astro';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';



<PreCheck>
  - Ejecute el asistente de configuración `create astro` para crear un nuevo proyecto Astro
  - Iniciar el servidor Astro en modo de desarrollo (dev)
  - Vea una vista previa en directo de su sitio web en su navegador
</PreCheck>

## Inicie el asistente de configuración de Astro

La forma preferida de crear un nuevo sitio Astro es a través de nuestro asistente de configuración `create astro`.

1. En la línea de comandos de su terminal, ejecute el siguiente comando utilizando su gestor de paquetes preferido:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      # crear un nuevo proyecto con npm
      npm create astro@latest
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      # crear un nuevo proyecto con pnpm
      pnpm create astro@latest
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      # crear un nuevo proyecto con yarn
      yarn create astro
      ```
      </Fragment>
    </PackageManagerTabs>

2. Confirme `y` para instalar `create-astro`.
3. Cuando se le pregunte "¿Dónde desea crear su nuevo proyecto?", escriba el nombre de una carpeta para crear un nuevo directorio para su proyecto, por ejemplo
`./tutorial`

    :::note
    Un nuevo proyecto Astro sólo puede crearse en una carpeta completamente vacía, así que elige un nombre para tu carpeta que no exista ya.
    :::

4. Verás una pequeña lista de plantillas de inicio entre las que puedes elegir. Utilice las teclas de flecha (arriba y abajo) para navegar hasta la plantilla "Vacío" y, a continuación, pulse return (Enter) para enviar su elección. 

5. Cuando aparezca la pregunta "¿Desea instalar las dependencias?", escriba `y`.

6. Cuando el prompt te pregunte si planeas escribir TypeScript, teclea `n`. 

7. Cuando se le pregunte: "¿Desea inicializar un nuevo repositorio git?", escriba `y`.

Cuando finalice el asistente de instalación, ya no necesitará este terminal. Ahora puede abrir VS Code para continuar.

## Abra su proyecto en VS Code

1. Abra VS Code. Se le pedirá que abra una carpeta. Elija la carpeta que creó durante el asistente de instalación.

2. Si es la primera vez que abre un proyecto Astro, debería ver una notificación preguntándole si desea instalar las extensiones recomendadas. Haz clic para ver las extensiones recomendadas y elige [Astro language support extension](https://marketplace.visualstudio.com/items?itemName=astro-build.astro-vscode). Esto proporcionará resaltado de sintaxis y autocompletado para tu código Astro.

3. Asegúrese de que el terminal está visible y de que puede ver el símbolo del sistema, por ejemplo:

    ```sh
    user@machine:~/tutorial$
    ```

    :::tip[Atajo de teclado]
    Para cambiar la visibilidad del terminal, utilice <kbd>Ctrl + J</kbd> (macOS: <kbd>Cmd ⌘ + J</kbd>).
    :::

Ahora puede utilizar el terminal integrado en esta ventana, en lugar de la aplicación Terminal de su ordenador, para el resto de este tutorial.



## Ejecutar Astro en modo de desarrollo

Para poder previsualizar los archivos de tu proyecto _como un sitio web_ mientras trabajas, necesitarás que Astro se ejecute en modo desarrollo (dev).

### Iniciar el servidor de desarrollo

1. Ejecute el comando para iniciar el servidor Astro dev escribiendo en el terminal de VS Code:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      npm run dev
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      pnpm run dev
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      yarn run dev
      ```
      </Fragment>
    </PackageManagerTabs>

    Ahora deberías ver la confirmación en el terminal de que Astro se está ejecutando en modo dev. 🚀

## Ver una vista previa de su sitio web

Tus archivos de proyecto contienen todo el código necesario para mostrar un sitio web Astro, pero el navegador es el responsable de mostrar tu código como páginas web.

1. Haz clic en el enlace `localhost` de tu ventana de terminal para ver una vista previa en directo de tu nuevo sitio web Astro. 

    (Astro utiliza `http://localhost:3000` por defecto si el puerto 3000 está disponible).

    Este es el aspecto que debería tener el sitio web de inicio del "Proyecto vacío" de Astro en la vista previa del navegador:

    ![Una página en blanco con la palabra Astro en la parte superior.](/tutorial/minimal.png)

:::tip[Uso del servidor de desarrollo Astro]

Mientras el servidor Astro esté funcionando en modo de desarrollo, NO podrás ejecutar comandos en tu ventana de terminal. En su lugar, este panel le dará retroalimentación mientras previsualiza su sitio.

Puede detener el servidor de desarrollo en cualquier momento y volver al símbolo del sistema escribiendo <kbd>Ctrl + C</kbd> en el terminal.

A veces el servidor de desarrollo se detendrá por sí mismo mientras estás trabajando. Si la vista previa en vivo deja de funcionar, vuelve al terminal y reinicia el servidor de desarrollo escribiendo `npm run dev`.
:::

<Box icon="check-list">
## Checklist

<Checklist>
- [ ] Puedo crear un nuevo proyecto Astro.
- [ ] Puedo iniciar el servidor de desarrollo Astro.
</Checklist>
</Box>

### Recursos

- <p>[Introducción a Visual Studio Code](https://code.visualstudio.com/docs/introvideos/basics) <Badge>externo</Badge> — un tutorial en vídeo para instalar, configurar y trabajar con VS Code</p>
