---
title: '404'
description: Houston, we have a problem.
editUrl: false
i18nReady: false
template: splash
---

import NotFound from '~/components/NotFound.astro'

<NotFound />

<script>
{`
  // Use IIFE to avoid polluting global scope
  (() => {
    const cssSelector = 'starlight-lang-select option[value="' + location.pathname + '"]';
    const matchedLanguageOption = document.querySelector(cssSelector);
    if (!!matchedLanguageOption && !!matchedLanguageOption.parentElement) {
      matchedLanguageOption.parentElement.value = matchedLanguageOption.value;
    }
  })();
`}
</script>
